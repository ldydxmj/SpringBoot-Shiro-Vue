<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.heeexy.example.dao.NovelDao">

    <insert id="addNovel" parameterType="com.alibaba.fastjson.JSONObject">
        INSERT INTO novel
            (author,peoples,has_gao_shou)
        VALUES
            (#{author},#{peoples},#{has_gao_shou})
    </insert>

    <select id="countNovel" resultType="Integer">
        SELECT count(0)
        FROM novel w
        WHERE w.delete_status = '1'
    </select>

    <select id="listNovel" resultType="com.alibaba.fastjson.JSONObject">
        SELECT
            w.id                                      id,
            w.author                          author,
            w.peoples                                 peoples,
            w.has_gao_shou                          has_gao_shou,
            date_format(w.create_time, '%Y.%m.%d %T') createTime
        FROM novel w
        WHERE w.delete_status = '1'
        ORDER BY w.id DESC
        LIMIT #{offSet}, #{pageRow}
    </select>

    <update id="updateNovel" parameterType="com.alibaba.fastjson.JSONObject">
        UPDATE novel
        SET
            peoples = #{peoples},
             has_gao_shou = #{has_gao_shou},
            author = #{author}
        WHERE id = #{id}
    </update>

</mapper>