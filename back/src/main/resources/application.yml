spring:
  datasource:
      url: jdbc:mysql://47.113.82.122:3306/example?useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&serverTimezone=UTC
      username: root
      password: root
  devtools:
    livereload:
      enabled: true #是否支持livereload
      port: 35729
    restart:
      enabled: true #是否支持热部署
#   resources:
#      static-locations:  classpath:/static/dist/
mybatis:
   mapperLocations: classpath:/mapper/*.xml

